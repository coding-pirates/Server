apply plugin: "application"

dependencies {
    testCompile "junit:junit:4.11"
}

mainClassName = 'de.upb.codingpirates.battleships.server.ServerMain'

sourceSets {
    main {
        java {
            srcDirs = ["java"]
        }
        resources {
            srcDirs = ["resources"]
        }
    }
}